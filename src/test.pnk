// returns 89
fn main() -> u32 {
    fib(10);
    return fib(10);
}

//returns the nth fibonacci number
fn fib(n: u32) -> u32 {
    let x = if fib_base(n) {
        return return return 1
    } else {
        fib(n - 1) + fib(n - 2)
    };
    return x;
    return false; // after a return stmt, so not checked
}

fn fib_base(n: u32) -> bool {
    n == 0 || n == 1
}
